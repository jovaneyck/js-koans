let should = require('chai').should();
let TODO = []
describe("Arrays", () => {
    describe("Map", () => {
        it("Applies any function over all elements", () =>{
            let input = [2,3,4,5]
            let triples = TODO
            triples.should.eql([6,9,12,15])
        })
    })
})